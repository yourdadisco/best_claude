# CLAUDE.md

本文件定义 Claude Code 在本仓库中的长期工作准则。目标是：在尽量少打扰用户的前提下，交付可验证、可维护、范围可控的成果。

## 项目事实

首次进入项目时，从 `README`、依赖清单、脚本和现有代码中确认以下信息；只记录事实，不猜测。发现内容过期时随变更更新。

- **项目目标**：<!-- 用一句话填写 -->
- **技术栈**：<!-- 只列实际使用的技术 -->
- **关键目录**：<!-- 只列不容易从目录名看出的边界 -->
- **开发命令**：`<!-- command -->`
- **定向测试**：`<!-- command -->`
- **完整测试**：`<!-- command -->`
- **检查与构建**：`<!-- lint / typecheck / build -->`

## 工作原则

1. 用户意图优先，其次是仓库事实与既有约定。遇到冲突或会显著改变产品行为的歧义时才提问；其余情况采用最合理的可逆假设继续推进，并说明假设。
2. 先理解再修改。开始时查看 `git status`、相关代码、相邻测试和必要文档；不要无目的地扫描整个仓库，也不要一次读入大文件。
3. 小改动直接做；非平凡任务按“计划与续作”执行。只有高代价、不可逆或方向不明确的决策需要额外等待用户批准，不强制为每个任务写完整 PRD。
4. 一次完成一个可验证的小切片。遵循现有架构和风格，只改完成当前目标所需的范围；不要顺手重构、改 UI、换技术栈或增加依赖。
5. 优先选择成熟、文档充分、容易测试和回退的方案，不为追新引入复杂度。抽象必须解决已经出现的问题，不为假想需求预留体系。
6. 人负责目标与验收，Claude 负责实现与举证。不要把“代码已写”当作“任务完成”。

## 计划与续作

- 新功能、多文件修改、架构/数据流变化或原因不明的复杂问题，先进入 Plan Mode：阅读相关代码，梳理现有流程，形成包含目标、验收条件、非目标、影响范围、3–7 个步骤和验证方式的短计划；用户确认后再开发。明确的单文件小改或局部修复可跳过。
- 非平凡任务将计划写入仓库根目录 `PLAN.md`，作为唯一的当前任务状态；步骤只标记 `待处理 / 进行中 / 已完成 / 阻塞`，并记录当前证据和下一步，不写冗长过程。
- 每完成一个可验证小切片立即更新 `PLAN.md`；长时间操作或高风险修改前也先更新，使异常中断最多丢失当前切片。
- 新会话开始时先检查 `PLAN.md`。若状态未完成，结合 `git status`、diff 和最快的相关测试校验真实进度，修正过期记录后从第一个未完成步骤继续，不重做已验证工作，也不盲信计划文件。
- 全部验收通过后把状态标为 `已完成`，写入最终验证结果；开始新任务时才替换已完成的旧计划。

## 渐进式披露

- 根 `CLAUDE.md` 只保留每次任务都需要的事实和原则，目标始终少于 200 行。
- 某类规则只对特定目录或文件生效时，移到 `.claude/rules/*.md`，使用 `paths` 限定范围；不要继续扩充根文件。
- 多步骤、偶尔使用的流程放入 Skill，仅在任务相关或用户点名时加载。若已安装 `grillme`，只在需求模糊、方案取舍会影响产品，或用户要求时使用。
- 子目录可放自己的 `CLAUDE.md`，只描述该模块特有的命令、边界和约定；不要复制根规则。
- 优先用搜索、符号定位和小范围读取获取上下文。对话变长时保留目标、决策、当前状态和验证证据，舍弃无关探索过程，不因上下文管理打断用户。

## 新项目或大型功能（按需触发）

写第一行实现代码前，确认足够的信息即可，不追求文档齐全：

- 谁在什么场景解决什么问题；核心功能及明确的验收标准是什么。
- UI 任务先确认主要页面、用户流程和参考风格，避免开发中反复推翻视觉方向。
- 明确边界及非功能要求：是否公开上线，是否涉及用户数据、支付或隐私，以及安全、性能、可用性和成本要求。
- 技术栈应可验证；先提出最小架构草案，说明模块边界和数据流。架构可以演进，但重大偏离要记录原因。
- 需求、架构和当前进度只有在确实有长期价值时才写入 `docs/`；重大变更后同步更新，禁止让文档维持错误的“旧世界观”。
- 使用 Git 保存安全检查点。除非用户明确要求，不自动提交、推送、重写历史或创建分支。

## 实现与代码质量

- 保持模块职责单一、接口明确；避免巨型文件和跨层耦合，但不使用僵硬的行数限制。
- 复用已有工具和模式。新增依赖前确认现有能力不能合理解决，并说明代价。
- 在系统边界验证输入，在产生错误的层处理错误；不要静默吞错或用虚假回退掩盖失败。
- 保留用户已有改动。不要使用破坏性 Git 命令，不要改写与任务无关的文件。
- 注释解释原因和约束，不复述代码。文档只记录真实、稳定、未来仍有用的信息。

## 测试与调试

- 行为变更必须有与风险相称的验证。Bug 修复应先复现；适合自动化时遵循 Red → Green → Refactor：先写会失败的测试，再做最小实现使其通过，最后整理代码。
- 先运行最相关、最快的测试，再按影响范围运行 lint、类型检查、完整测试或构建。不得靠删除测试、放宽断言、跳过校验或吞掉异常来“通过”。
- 调试遵循：收集完整错误 → 最小复现 → 提出单一假设 → 最小修改 → 再验证。一次只改变一个关键变量。
- 若连续两次尝试没有产生新证据或缩小问题范围，停止盲改，重新检查调用链、数据流、日志和环境假设；仍无法推进时，报告证据、已排除项和一个具体阻塞点。

## 模拟真人验收循环

- **触发**：页面、交互或完整用户流程发生变化时，自动化检查通过后使用 Browser、Chrome 或 Computer Use 从可见界面验收；接口、DOM 和代码检查不能代替。纯文档、内部重构及无 UI 的后端改动跳过。
- **执行**：启动真实应用，从干净状态完成“点击/输入/提交 → 观察 → 对照验收 → 修复 → 完整复验”；覆盖主路径，必要时补一个关键失败路径并刷新确认状态。
- **停止**：只有实际观察到结果才能记 `PASS`；工具或环境不可用记 `BLOCKED`。连续两轮失败且没有新证据时停止并报告。
- **留证**：首次触发时创建 `log/`，写入 `log/human-validation-YYYYMMDD-HHmm.md`，只记 `时间/环境/工具、目标、步骤与观察、PASS|FAIL|BLOCKED、可选证据`；禁止记录任何秘密信息。

## 安全底线

- 密钥、令牌和密码不得进入前端、仓库、日志或错误信息；使用环境变量和服务端调用，并维护安全的示例配置。
- 不信任客户端输入。服务端必须执行认证、授权、输入校验和必要的速率限制；数据库访问使用参数化查询或可靠 ORM。
- 涉及删除、迁移、生产环境、权限或外部付费操作时，先核对精确目标、影响和回退方式；权限不足时停止并说明。

## 完成标准

声称完成前必须：

1. 逐项核对验收条件，并实际运行相关测试/检查/构建。
2. 查看最终 diff，确认没有调试代码、秘密信息、无关修改或意外生成文件。
3. 对重要变更做一次独立视角自审：正确性、边界条件、安全性、兼容性和测试缺口。
4. 简洁汇报改了什么、验证结果及仍存在的风险；没有运行的检查必须明确写出，不能用推测代替证据。

## 维护本文件

只在以下信息会跨会话反复有用时更新：真实命令、稳定架构边界、项目专属约定，以及同类错误第二次出现后形成的规则。新增规则前先删除重复、过期或 Claude 可从代码直接推断的内容。
